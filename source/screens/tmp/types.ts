import { AppStackParams } from '../../navigation/AppNavigator/types';
import { NativeStackScreenProps } from '@react-navigation/native-stack';

export type HomeProps = NativeStackScreenProps<AppStackParams, 'Home'>;
